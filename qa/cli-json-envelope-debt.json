{
  "schemaVersion": 1,
  "description": "Pre-V1 raw CLI JSON writers that still need migration to the common {ok,data,error,meta} envelope. Counts are maximums: new raw writers fail policy, and migrations must lower the relevant count.",
  "files": {
    "packages/clawjs/src/index.ts": 49
  }
}
