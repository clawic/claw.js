{
  "name": "clawjs-vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/bin/server.ts",
    "build:ui": "npm --prefix ui run build",
    "build:server": "tsup src/bin/server.ts src/bin/sidecar.ts --format esm --target node20 --out-dir dist --clean --external @playwright/test --external playwright-core --external chromium-bidi/*",
    "build": "npm run build:ui && npm run build:server",
    "start": "node dist/server.js",
    "sidecar": "node dist/sidecar.js",
    "test": "tsx --test tests/e2e/backend.e2e.test.ts",
    "test:e2e": "npm run build && playwright test -c playwright.config.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^8.1.0",
    "better-sqlite3": "^12.4.1",
    "fastify": "^5.6.1",
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^22.19.15",
    "tsup": "^8.5.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
