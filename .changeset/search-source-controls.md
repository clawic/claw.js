---
"@clawjs/search": minor
"@clawjs/claw": minor
---

Add Root Search source controls so sources can be paused, resumed, disabled, or excluded without losing their persisted state during rebuilds.
Expose `createRootSearchFederator()` as the named Root Search API while keeping `createSearchRegistry()` as a compatible alias.
