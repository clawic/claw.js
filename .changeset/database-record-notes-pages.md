---
"@clawjs/database": patch
---

Store record `notes` and `notesBody` payload fields as associated Pages with blocks, revisions, and note search rows instead of embedding long-form text in record JSON.
