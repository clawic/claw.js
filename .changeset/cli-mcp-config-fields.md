---
"@clawjs/cli": minor
---

Preserve additional MCP server configuration fields in `claw mcp upsert`, including working directories, passthrough environment variables, bearer token environment variables, headers, header environment references, and enabled state.
