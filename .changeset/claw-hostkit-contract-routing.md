---
"@clawjs/core": patch
"@clawjs/cli": patch
---

Add v1 host contract fixtures, JSON schema exports, Codex read-only safety checks, domain ownership enforcement, active-host command forwarding for brokered CLI domains, and a deprecation warning for the legacy `clawjs` alias.
