---
"@clawjs/core": patch
"@clawjs/cli": patch
---

Export the v1 domain ownership matrix from core and normalize the CLI runtime bin name to `claw`, while warning when the deprecated `clawjs` bin is invoked.
