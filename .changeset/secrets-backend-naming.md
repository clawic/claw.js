---
"@clawjs/claw": patch
"@clawjs/core": patch
"@clawjs/node": patch
---

Rename the Vault-backed secrets configuration surface to Secrets naming, including `secrets` backend selection, `SECRETS_*` environment variables, and `secrets:` auth summaries.
