---
"@clawjs/search": minor
---

Add a physical Search shard catalog in `search.sqlite` that tracks per-source shard state plus document and fragment counts while preserving existing shard-scoped queries.
