---
"@clawjs/database": patch
---

Default the database service to the shared ClawJS main data directory and `clawjs.sqlite` file, with explicit environment overrides for the main database and file store.
