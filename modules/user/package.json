{
  "name": "user",
  "version": "0.1.0",
  "description": "Cross-service user explorer dashboard for ClawJS",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/ui.html dist/ui.html",
    "test": "npm run build && vitest run --config vitest.config.ts --passWithNoTests"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3"
  }
}
