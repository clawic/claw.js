# Device Tests

Device or simulator validation scenarios live here. They run only through
`npm run test:device` with an explicit `CLAW_DEVICE_TEST_COMMAND`; otherwise the
lane reports `EXTERNAL PENDING`.
