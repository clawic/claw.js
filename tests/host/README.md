# Host Tests

Signed host validation scenarios and fixtures live here. They run only through
`npm run test:host` or release gates with an explicit `CLAW_HOST_TEST_COMMAND`;
otherwise the lane reports `EXTERNAL PENDING`.
