---
title: Decision Map
description: Canonical architecture, naming, storage, validation, and release decisions with their protecting checks.
---

# Decision Map

This map tells contributors and agents where durable ClawJS/Clawix decisions
live and which check or review step protects them. It is an index, not a second
source of truth: update the canonical document first, then update this map.
The contract format is decision -> document -> validation.
`scripts/docs-rendered-link-check.mjs` builds the VitePress docs and verifies
that this map and every ADR render with valid source and rendered links.

## Architecture and ownership

| Decision | Canonical document | Guardrail or validation |
| --- | --- | --- |
| ClawJS/Claw owns framework contracts, v1 schemas, fixtures, storage resolution, domain APIs, command routing, and the public `claw` CLI. | [Host Ownership](./host-ownership.md), [ADR 0001: framework and host boundary](./adr/0001-claw-framework-host-boundary.md), `packages/clawjs-core/src/domain-ownership.ts` | `npm run test:docs` runs `scripts/docs-alignment-check.mjs`; `scripts/naming-surface-guard.mjs` blocks stale public CLI and storage surfaces. |
| `claw` is the only public CLI; public `clawjs`, `clawix`, and legacy command names are not new framework surfaces. | [Naming Style Guide](./naming-style-guide.md), [ADR 0001: naming and stability surfaces](./adr/0001-naming-and-stability-surfaces.md), [CLI](./cli.md) | `scripts/naming-surface-guard.mjs` verifies `packages/clawjs/package.json` exposes exactly the `claw` bin and blocks retired names. |
| `claw` is the canonical agent interface to the framework; stable commands, aliases, support states, security policy, docs, ADRs, tests, and source links are registry-backed and inspectable. | Root `CONSTITUTION.md`, [ADR 0007: CLI agent interface](./adr/0007-cli-agent-interface.md), [CLI](./cli.md), `packages/clawjs-core/src/cli-command-registry.ts` | CLI and inspect tests cover registry-backed help, aliases, `inspect why`, deterministic `search`, and unknown-command related matches. Future gates must fail registry/docs/test drift. |
| CLI action vocabulary is a first-class registry: arbitrary command phrases resolve to `covered`, `candidate_alias`, `gap`, `future`, `blocked`, or `external_pending` without executing unknown behavior. | Root `CONSTITUTION.md`, [ADR 0018: CLI action intent registry](./adr/0018-cli-action-intent-registry.md), [CLI](./cli.md), `packages/clawjs-core/src/cli-command-intents.ts` | `claw commands resolve|record|list|opportunities|promote`, audited collection-alias resolution such as `lead list -> db leads list`, `claw inspect command-intents`, unknown-command `meta.commandIntent`, `cli.commandIntentResolution`, and tests in `cli-command-intents.test.ts`, `cli-commands.test.ts`, `cli-discovery.test.ts`, and `inspect-cli.test.ts` protect the contract. |
| Search is the public framework capability for querying entities, conversations, commands, saved searches, monitors, and optional external sources; each source owns a fast path and Root Search federates sources with strict timeouts. | [Search](./search.md), [ADR 0019: Search V1.1 architecture](./adr/0019-search-v1-1-architecture.md), `@clawjs/search`, `packages/clawjs/src/cli-search-command.ts` | Search package tests cover manifests, Root Search timeouts, lexical matching, and the SQLite store. CLI tests cover `claw search query|sources|status|rebuild|saved|monitors|actions|profiles|explain` and source projections for commands, sessions, and database records. |
| Agents are the canonical framework domain for durable digital employees; assignments, not deployments, define where an agent acts, and effective access is a fail-closed intersection across agent, assignment, execution profile, connector, host, and run scope. | [ADR 0020: Agents V1 refactor](./adr/0020-agents-v1-refactor.md), [ADR 0012: surface route graph](./adr/0012-surface-route-graph.md), `packages/clawjs-core/src/agents-v1.ts`, `packages/clawjs-core/src/builtins/agents/` | `packages/clawjs-core/src/agents-v1.test.ts`, built-in registry tests, `packages/clawjs/src/index-data.test.ts`, and inspect route tests cover schemas, CLI, fail-closed grants, delegation no-laundering, budget gates, redaction, safe package export, safe Relay/MCP/API surface projections, route graph visibility, and `claw inspect agent <id>` fiches. |
| High-density professional domains use one dense data operating system: visible packs and direct human CLI nouns orchestrate shared `core.sqlite` collections, evidence, provenance, quality gaps, relations, semantic views, and intent coverage without creating parallel databases. | [ADR 0021: dense data operating system](./adr/0021-dense-data-operating-system.md), [Dense Data Decision Matrix](./dense-data-decision-matrix.md), [Dense Data Source Decision Audit](./dense-data-source-decision-audit.md), [Dense Data Existing Catalog Audit](./dense-data-existing-catalog-audit.md), [Canonical Data Catalog](./canonical-data-catalog.md), `packages/clawjs-core/src/dense-data-os.ts`, `packages/clawjs-core/src/dense-data-fixtures.ts`, `packages/clawjs-core/src/domain-surface-registry.ts`, `packages/clawjs-core/src/builtins/data_foundation/`, `packages/clawjs/src/cli-dense-data-command.ts` | `packages/clawjs-core/src/dense-data-os.test.ts` verifies source metadata, intent statuses, foundation primitive-to-collection mappings, generated dense intent entries, semantic views, acceptance fixture coverage, first-wave dense systems, acronym aliases, direct center nouns, patient medication routes, ERP orchestration, roadmap visibility, and registry completeness. `scripts/verify-dense-data-goal.mjs` keeps the public docs privacy-safe and checks source decision rows, dense matrix rows, first-wave systems, foundation mappings, generated intents, semantic views, fixtures, `dense-fixtures` registry coverage, materialized patient, study, case, service, sample, experiment, learner, course, company, asset, and work-order timelines, ERP company overview, CRM account overview, finance entity overview docs, and `external_pending` requirements. `packages/clawjs-core/src/domain-surface-registry.test.ts` verifies `dense-system:*` inspection coverage. `packages/clawjs/src/inspect-cli.test.ts` verifies `claw inspect dense-data|dense-intents|dense-views|dense-fixtures`. `packages/clawjs/src/cli-discovery.test.ts` verifies known direct CLI nouns such as `patient list`, `patient <id> medications list`, `company create`, `evidence-source create`, `quality-gap create`, `patient <id> timeline`, `study <id> timeline`, `case <id> timeline`, `service <id> timeline`, `sample <id> timeline`, `experiment <id> timeline`, `learner <id> timeline`, `course <id> timeline`, `company <id> timeline`, `asset <id> timeline`, `work-order <id> timeline`, `erp company <id> overview`, `crm account <id> overview`, `finance entity <id> overview`, and `dense-fixtures seed` execute or report structured dense-data coverage instead of unknown-command output. The source decision audit, decision matrix, and existing-catalog audit track source user decisions through implementation, docs, tests, integration choices, and remaining blockers. |
| Every stored domain has an explicit route from database/storage to collections, signals, aggregates, systems, runtime services, packages, module manifests, portals/aliases, CLI, and host boundaries. | [ADR 0012: domain surface registry from database to CLI](./adr/0012-domain-surface-registry-db-to-cli.md), [Domain Surface Decision Matrix](./domain-surface-decision-matrix.md), `packages/clawjs-core/src/domain-surface-registry.ts`, [Canonical Data Catalog](./canonical-data-catalog.md), [Signals Modules](./signals-modules.md) | Core tests and `scripts/domain-surface-registry-guard.mjs` fail missing registry coverage for collections, signals, services, storage, CLI, missing `module.json` manifests, or conceptual module package wrappers. `scripts/verify-domain-surface-decision-matrix.mjs` keeps the decision evidence matrix complete. |
| Agents work from registered surfaces and explicit transverse routes, with Relay, bridge, daemon, runtime, sessions, CLI, MCP, storage, permissions, grants, approvals, audit, and public contracts visible through the same graph. | [ADR 0012: surface route graph](./adr/0012-surface-route-graph.md), [ADR 0004: stable surface registry and inspection](./adr/0004-persistent-surface-registry-and-inspection.md), `packages/clawjs-core/src/surface-registry.ts` | `claw inspect show|neighbors|routes|route|agent` exposes fiches, adjacent nodes, edges, route steps, contracts, tests, and gaps. `scripts/surface-route-graph-guard.mjs` fails missing critical nodes, broken edge/route references, missing route tests, or missing validation. |
| Remote access is the governed projection of local framework contracts, not a parallel Relay API; Coordinator, Gateway, Connector, Sync, Iroh adapter, headless hosts, remote parity, secret-reference leases, sync manifests, and the opt-in durable sync ledger are first-class route graph surfaces. | [Relay](./relay.md), [Interface Matrix](./interface-matrix.md), [ADR 0022: remote gateway and sync redesign](./adr/0022-remote-gateway-sync-redesign.md), [Remote Gateway And Sync Decision Matrix](./remote-gateway-sync-decision-matrix.md), `packages/clawjs-core/src/remote-sync.ts`, `packages/clawjs-core/src/surface-registry.ts`, `packages/clawjs/src/remote-sync-state-store.ts`, `relay/src/server/remote-sync-routes.ts` | `claw remote classify|check|routes|conformance`, `claw sync manifest|status|plan|run|reconcile|conflicts`, `claw nodes list|pair|trust|revoke|invite|share|heartbeat`, `claw gateway serve|project|conformance`, Relay `/v1/remote/*`, `/v1/gateway/conformance`, `/v1/sync/*`, and `/v1/nodes*` routes, `claw inspect routes`, `scripts/surface-route-graph-guard.mjs`, `scripts/verify-remote-sync-goal.mjs`, and core/inspect/Relay tests protect the baseline. |
| Local macOS control is a first-class governed host capability: direct CLI roots such as `wifi`, `window`, `shortcut`, `app`, and `permissions` resolve through the Mac capability atlas, central Mac Permission Broker, signed-host Mac Action Broker, redacted audit receipts, and conflict-aware help. | [Mac Control Plane](./mac-control-plane.md), [ADR 0023: Mac Control Plane V1](./adr/0023-mac-control-plane-v1.md), [ADR 0024: Mac Permission Broker V1](./adr/0024-mac-permission-broker-v1.md), `packages/clawjs-core/src/mac-control-plane.ts`, `packages/clawjs/src/cli-mac-control-command.ts` | `packages/clawjs-core/src/mac-control-plane.test.ts` validates atlas, roles, risk, permissions, receipts, grants, and V1 slice contracts. `packages/clawjs/src/cli-mac-control-command.test.ts` validates direct roots, dry-run plans, permission request plans, and collision `Related surfaces`. `scripts/discoverability-check.mjs` and `scripts/verify-cli-registry-router-parity.mjs` protect docs and help registration. |
| Durable meta-code must be discoverable from `AGENTS.md`/`CLAUDE.md`, relevant skills, docs routers, and `claw search`/`claw inspect` within two hops; inherited hidden artifacts are explicit expiring debt. | [ADR 0017: discoverability and meta-code routing](./adr/0017-discoverability-and-meta-code-routing.md), `docs/discoverability.registry.json`, `docs/discoverability-baseline.json` | `scripts/discoverability-check.mjs` validates route distance, ADR registry/baseline coverage, skill frontmatter, search query evidence, inspect references, and decision-like comment references. |
| CLI command attempts may return compact just-in-time guidance, verified actor identity metadata, and opaque `res_*` resource references without injecting long docs or secrets into context. | [ADR 0010: CLI just-in-time guidance, actor assertions, and resource registry](./adr/0010-cli-jit-guidance-actor-assertions-resource-registry.md), [CLI](./cli.md), `packages/clawjs-core/src/cli-guidance.ts` | Unit and CLI tests cover guidance matching, actor assertion verification, resources registration, and JSON `meta.guidance`. |
| `Claw.app` is the standalone signed macOS host; Clawix is a human interface and embedded signed host through `ClawHostKit`. | [Host Ownership](./host-ownership.md), [ADR 0001: framework and host boundary](./adr/0001-claw-framework-host-boundary.md) | `scripts/docs-alignment-check.mjs` requires `Claw.app`, `ClawHostKit`, and ownership language in the canonical docs. |
| Sensitive native permissions, approvals, destructive grants, cost-bearing decisions, native secrets, and host audit logs belong to the active signed host, not Node. | [Host Ownership](./host-ownership.md), [ADR 0001: framework and host boundary](./adr/0001-claw-framework-host-boundary.md) | `scripts/verify-host-permission-contract.mjs` blocks Node implementation code from adding direct TCC/computer-use permission calls outside the audited bridge broker. Dry-run validation counts only as partial validation for native permission behavior. |
| Secrets V1 uses password + Secret Key, Emergency Kit recovery, device-local Keychain material only, Secrets-only macOS XPC assertions, brokered automation, and no plaintext agent/CLI/plugin access. | [Secrets Security](./secrets-security.md), [ADR 0008: Secrets Security V1](./adr/0008-secrets-security-v1.md), [Host Ownership](./host-ownership.md) | `npm run secrets:security`, Clawix `SecretsSecurityBoundaryTests`, and `macos/scripts/verify_sidecar_host.sh` cover the Mac + ClawJS V1 boundary. |
| Secrets use a hostile-local-process threat model: humans reveal through the signed host, while agents, connectors, plugins, automation, SDKs, and CLI use references and brokered actions without plaintext values. | [Secrets Security Model](./secrets-security.md), [Secrets](./secrets.md), [Host Ownership](./host-ownership.md) | Review secret-related changes against the checklist in `docs/secrets-security.md`. Future guardrails must block generic reveal, missing-context allow, and direct connector plaintext resolution. |
| External APIs, SDKs, MCP tools, CLIs, webhooks, OAuth/browser flows, and provider endpoints go through the Connector Control Plane before runtime execution. | [Connector Control Plane](./connector-control-plane.md), [ADR 0015: Connector Control Plane V1](./adr/0015-connector-control-plane-v1.md), `packages/clawjs-core/src/connector-control-plane.ts` | Core tests cover capability IDs, deny-wins policy, budgets, unknown cost grants, network proof, credential bindings, audit trace mode, and strict V1 blocking. Catalog and live checks remain fixture/brokered and use `EXTERNAL PENDING` for unavailable real providers. |

## Storage and data placement

| Decision | Canonical document | Guardrail or validation |
| --- | --- | --- |
| Framework global data, framework databases, workspace files, host state, and GUI-only app state have separate roots. | [Data Storage Boundary](./data-storage-boundary.md), [Host Ownership](./host-ownership.md), `packages/clawjs-core/src/storage.ts` | `scripts/docs-alignment-check.mjs` and `scripts/naming-surface-guard.mjs` require current roots and block stale pre-refactor roots. |
| Durable paths, databases, sidecars, host operational paths, external read-only sources, preference keys, app/browser storage keys, tables, columns, indexes, caches, fixtures, persistent temps, API routes, webhooks, events, queues, schemas, JSON fields, protocol frames, CLI commands/flags, ID namespaces, deep links, hostnames, ports, and external mappings are registered through typed builders and inspected through `claw inspect`. | [ADR 0004: stable surface registry and inspection](./adr/0004-persistent-surface-registry-and-inspection.md), [Persistent Surface](./persistent-surface.md), `packages/clawjs-core/src/surface-registry.ts` | Core tests cover registry lookup/tree behavior; CLI tests cover `claw inspect tree`, `show`, `apis`, `protocols`, `ids`, generated Markdown/Mermaid output, and static manifest fusion. `scripts/persistent-surface-guard.mjs` blocks unregistered durable and stable contract names. |
| New workspace-local framework writes use `.claw/`; `.clawjs/` is retired pre-public compatibility. | [Workspace](./workspace.md), [Data Storage Boundary](./data-storage-boundary.md), [Naming Style Guide](./naming-style-guide.md) | `scripts/naming-surface-guard.mjs` blocks retired `.clawjs` workspace paths in docs, selected canonical sources, and implementation code. |
| User-facing structured records belong in `core.sqlite`; high-churn runtime/search/blob state uses sidecars; plaintext secrets never live in the main database. | [Data Storage Boundary](./data-storage-boundary.md), [Database](./database.md), [Secrets](./secrets.md), [Secrets Security Model](./secrets-security.md) | `scripts/docs-alignment-check.mjs` requires `core.sqlite`, sidecar names, and plaintext secret language. Service tests cover storage behavior for individual packages. |
| Built-in collections follow the canonical data catalog: broad human-recognizable coverage, standardized names, sparse fields, semantic relations, metadata evidence, and conservative evolution. | [Canonical Data Catalog](./canonical-data-catalog.md), [ADR 0005: canonical data catalog](./adr/0005-canonical-data-catalog.md), [Database](./database.md), root `CONSTITUTION.md`, `packages/clawjs-core/src/builtins/_types.ts` | Built-in catalog tests require zero metadata debt, validate aliases, validate required-field reasons, reject generic built-in relation semantics, and keep CRUD smoke coverage for all built-ins. |
| Codex data under `~/.codex` is an external read-only source by default. | [Host Ownership](./host-ownership.md), [ADR 0001: framework and host boundary](./adr/0001-claw-framework-host-boundary.md) | Review integrations for read/mirror/index behavior only. Writes into Codex-owned sources require an explicit reversible opt-in outside ordinary framework code. |

## Runtime, bridge, and validation

| Decision | Canonical document | Guardrail or validation |
| --- | --- | --- |
| Runtime, host, and bridge contracts are framework-owned and consumed by hosts rather than duplicated in UI clients. | [Runtime](./runtime.md), [Execution](./execution.md), [Host Ownership](./host-ownership.md), [Interface Matrix](./interface-matrix.md) | `npm run test:docs` checks public surface docs against `surface-contract.registry.json`; package tests cover contract fixtures and exports. |
| Host-dependent bugs require host-equivalent validation when feasible, not only hermetic tests. | Root `AGENTS.md`, [Diagnostics](./diagnostics.md), root `tests/e2e/README.md` | Final reports must distinguish real host validation from hermetic fixture validation. `CLAW_E2E` fixture paths are not proof of host behavior by themselves. |
| Tests and examples must not send operative prompts, mutate production data, touch real services, or consume paid APIs unless explicitly approved. | Root `CLAUDE.md`, root `AGENTS.md`, [Getting Started](./getting-started.md), [Content](./content.md) | Use fixtures, mocks, local services, and dry-run paths. Mark unavailable real integrations as external validation pending. |
| External connectors are complete only when their official provider surface is mapped, classified, fixture-tested, and live/manual limits are explicit. | [Integration QA Lab](./integration-qa-lab.md), [Integration QA Reference Research](./integration-qa-reference-research.md), [ADR 0006: integration QA lab](./adr/0006-integration-qa-lab.md), `packages/clawjs-integrations/src/telegram-official-api-matrix.ts` | `packages/clawjs-integrations/src/telegram-official-api-matrix.test.ts` verifies the Telegram Bot API matrix; live rows require `CLAW_TEST_LIVE=1`, brokered credential leases, and `EXTERNAL PENDING` for unavailable physical/provider prerequisites. |
| Integration QA scenarios use the standard report vocabulary and must keep sensitive Telegram flows gated. | [Integration QA Lab](./integration-qa-lab.md), `qa/scenarios/telegram-integration-qa-lab.md`, `scripts/verify-integration-qa-scenarios.mjs` | `npm run test:qa-scenarios` builds the integration package and validates matrix counts, update coverage, live-smoke scenarios, manual/policy gates, and documented `PASS`/`FAIL`/`PARTIAL`/`EXTERNAL PENDING`/`QUARANTINED` status vocabulary. |
| Package/live connector validation installs the candidate package before any live broker command can receive lease context. | [Integration QA Lab](./integration-qa-lab.md), `scripts/verify-integration-package-live-harness.mjs`, `qa/scenarios/telegram-live-broker-runbook.md` | `npm run test:package-live` packs and installs local candidate tarballs into a temporary app, rejects raw Telegram token env vars, validates broker reports written to `CLAWJS_LIVE_REPORT_PATH`, and reports live/Docker prerequisites as `EXTERNAL PENDING` when not explicitly enabled. |
| Public API, SDK, CLI, and Relay surfaces stay aligned. | [Interface Matrix](./interface-matrix.md), [Public Surface](./surface.md), `docs/surface-contract.registry.json` | `npm run test:docs` runs `scripts/docs-surface-check.mjs` and `scripts/docs-alignment-check.mjs`. |

## Source file boundaries

| Decision | Canonical document | Guardrail or validation |
| --- | --- | --- |
| Hand-authored source files stay responsibility-scoped; new 1200+ line files require a split plan or baseline rationale, new 2000+ line files are blocked unless exempted, and emergency-debt files above 5000 lines must not grow except for extraction/deletion/split work. | [ADR 0003: source file boundaries](./adr/0003-source-file-boundaries.md), `docs/source-size-baseline.json` | `npm run test:docs` runs `scripts/source-size-check.mjs`; it warns at 800 lines, reports compression/export/baseline signals, blocks new 2000+ line files, and blocks emergency-debt or explicitly locked baseline growth. |
| Code hygiene removes clear dead code, orphan files, stale private exports, unused dependencies, stale assets, and unbounded cleanup debt only after public/canonical surfaces and report-only categories are classified. | [ADR 0016: code hygiene program](./adr/0016-code-hygiene-program.md), [Code Hygiene Report](./code-hygiene-report.md), [Code Hygiene Completion Audit](./code-hygiene-completion-audit.md), `docs/code-hygiene-decisions.json`, `docs/code-hygiene-baseline.json`, `docs/code-hygiene-ledger.md` | `scripts/code-hygiene-check.mjs` validates active artifacts, completion audit rows, baseline/report consistency, and non-destructive tool policy. `skills/code-hygiene-audit` audits without editing; `skills/code-hygiene-cleanup` executes one repo/category at a time. |

## Naming, release, privacy, and commits

| Decision | Canonical document | Guardrail or validation |
| --- | --- | --- |
| Public, persistent, source, docs, JSON/YAML, exported, and internal names follow the naming ADRs and the agentic vocabulary standard: contract fields use `camelCase`, CLI flags use `kebab-case`, SQL and collections use `snake_case`, events use `domain.action`, files follow ecosystem idioms, and shared domain words come from the vocabulary registry. | [Naming Style Guide](./naming-style-guide.md), [Agentic Naming Guide](./agentic-naming-guide.md), [Vocabulary Registry](./vocabulary.registry.json), [ADR 0001: naming and stability surfaces](./adr/0001-naming-and-stability-surfaces.md), [ADR 0013: agentic naming and code structure](./adr/0013-agentic-naming-and-code-structure.md) | `scripts/naming-surface-guard.mjs` freezes stable surface names. `scripts/naming-shape-check.mjs` blocks critical naming source drift and reports vocabulary, file, docs, JSON/YAML, and broad-symbol issues for staged cleanup. |
| Public repositories contain only safe placeholders and no secrets, signing identities, private paths, release credentials, or maintainer-specific configuration. | Root `AGENTS.md`, root `SECURITY.md`, [Host Ownership](./host-ownership.md) | `npm run privacy:check` and package smoke tests are part of `npm run ci`. Review generated artifacts before publishing or packing. |
| Changesets are release metadata for public package behavior, not standalone work units. | Root `AGENTS.md`, [Git Workflow](./git-workflow.md), root `RELEASING.md` | PR review requires changesets with public package behavior changes and keeps them with the functional change they document. |
| Publishing, release tags, uploads, and real package publication are explicit release actions, never ordinary validation side effects. | Root `RELEASING.md`, [Git Workflow](./git-workflow.md), root `package.json` | `npm run publish:dry-run` is the pre-publish check. Real `release:publish`, tags, and uploads require explicit release approval. |

## Known pending guardrails

No known pending guardrails are tracked in this map.
