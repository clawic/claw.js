# Dense Data Operating System Decision Matrix

Source conversation: `019e35a1-06bb-77f2-a712-92ed2646bd15`

Reference plan item: `019e3659-0335-7811-9cda-c9d176e91515-plan`

This matrix is the public, privacy-safe acceptance ledger for the dense data
operating system. It must be updated before the goal can close. Private session
paths and local goal files intentionally stay out of the public repository.

| ID | User decision | Implementation state | Canon/docs | Tests/evidence | Remaining work |
| --- | --- | --- | --- | --- | --- |
| DD-001 | Build a very ambitious, deeply reasoned data expansion, but avoid chaos, duplicated fields, and meaningless over-modeling. | In progress: `clawDenseDataOsRegistry` defines systems, shared engines, centers, operations, semantic views, statuses, and roadmap domains. | ADR 0021, decision map, canonical data catalog. | `dense-data-os.test.ts`, domain surface registry guard. | Complete first-wave packs beyond graduated centers; add stricter no-duplicate audit. |
| DD-002 | The goal is structure of data plus CLI, not UI. | Implemented for first slices: dense routes are CLI-only and use shared DB paths. | CLI docs and ADR 0021. | `cli-discovery.test.ts` covers direct CLI routes. | Extend docs/help for every graduated top-level noun. |
| DD-003 | Domains with specialized software and dense relational data must be handled as a class, not only medicine. | In progress: first-wave systems cover health/EHR, research/CTMS, biology, labs/LIMS, legal, ERP, CRM, finance/accounting, education/LMS, manufacturing/MES, ops/ITSM; roadmap covers HRIS, SCM, WMS, TMS, GRC, PIM/PLM, CMMS, ELN, and more. | ADR 0021, dense registry. | `dense-data-os.test.ts` verifies first-wave and roadmap visibility. | Graduate non-health/non-ERP systems to concrete collections and CLI smoke tests. |
| DD-004 | Completeness is measured by expected CLI intentions: `patient list`, save symptoms/medications, list a patient's medications, and equivalent professional intents. | In progress: `patient list/create/schema`, `medication add --patient`, `patient <id> medications list`, `patient <id> symptoms add/list`, `company/account/deal/invoice` routes now execute against canonical collections. | ADR 0021 and CLI docs. | `cli-discovery.test.ts` covers the graduated routes. | Add exhaustive per-pack intent registries and gap reports for ungraduated routes. |
| DD-005 | `claw patient list` must work without `health` in the command. | Implemented for patient; direct center nouns are supported by dense routing. | CLI docs, ADR 0021. | `cli-discovery.test.ts` asserts `patient create/list` route through `database`. | Add generated coverage for every audited collection singular/plural noun. |
| DD-006 | Do not go case-by-case when the pattern is inferable; apply the same model to 10-20 dense domains. | In progress: common registry and DB routing are generic; first concrete graduation now spans health and ERP/CRM. | ADR 0021. | Registry and CLI tests cover cross-domain systems. | Broaden semantic views, fixtures, and smoke tests across all first-wave systems. |
| DD-007 | The plan must include all three layers: mega-spec/canon, standards/registry thinking, and implementation. | In progress: registry/types/docs/tests exist and initial implementation routes are executable. | ADR 0021, decision map, CLI docs, this matrix. | Unit, CLI, docs, and registry guard validations. | Add inspect/database exposure and richer foundation primitives. |
| DD-008 | ERP, legal, and similar systems must be implemented at DB/system level, not just mentioned. | In progress: ERP/CRM have executable company/account/deal/invoice paths; legal has `case` plus case evidence; ops has `service` plus incidents linked to services. | ADR 0021, CLI docs. | ERP/CRM/legal/ops route coverage in `cli-discovery.test.ts`; built-in registry and smoke tests pass. | Graduate research/labs/education/manufacturing centers next. |
| DD-009 | No parallel system: review current notes, graph, knowledge, associations, signals, attachments, custom fields, CRM, billing, ERP, infra/ops before closing. | In progress: current work reuses built-in collections, `core.sqlite`, aliases, DB CRUD, and surface registry rather than a new store. | Goal file, ADR 0021, decision map. | Built-in registry/smoke tests plus domain surface guard. | Add explicit audit artifact for existing graph/knowledge/custom-field primitives and retire/merge decisions. |
| DD-010 | Data can be partial, but gaps must be explicit and not confused with bugs. | Partially implemented: dense intent statuses include `data_gap`, `workflow_gap`, `external_pending`, `blocked`; ungraduated dense routes return structured gaps. | ADR 0021. | `dense-data-os.test.ts`, `cli-discovery.test.ts` health gaps route. | Add first-class quality gap records/fixtures and `EXTERNAL PENDING` examples for real integrations. |
| DD-011 | The final goal must not close until every decision Q/A is reviewed one by one. | In progress: this matrix is seeded from the source user decision messages. | This document. | Manual source-session extraction performed during implementation. | Before completion, re-read the full source session and mark every row complete or blocked with evidence. |
