# Dense Data Operating System Decision Matrix

Source conversation: `019e35a1-06bb-77f2-a712-92ed2646bd15`

Reference plan item: `019e3659-0335-7811-9cda-c9d176e91515-plan`

This matrix is the public, privacy-safe acceptance ledger for the dense data
operating system. It must be updated before the goal can close. Private session
paths and local goal files intentionally stay out of the public repository. The
source-turn enumeration lives in
[Dense Data Source Decision Audit](./dense-data-source-decision-audit.md).
`scripts/verify-dense-data-goal.mjs` guards this ledger against missing source
decision rows, missing matrix rows, privacy-unsafe public paths, registry drift,
fixture drift, semantic-view drift, missing `external_pending` rows, and missing
existing-surface anti-duplication audit rows.

| ID | User decision | Implementation state | Canon/docs | Tests/evidence | Remaining work |
| --- | --- | --- | --- | --- | --- |
| DD-001 | Build a very ambitious, deeply reasoned data expansion, but avoid chaos, duplicated fields, and meaningless over-modeling. | In progress: `clawDenseDataOsRegistry` defines systems, shared engines, centers, operations, semantic views, statuses, roadmap domains, and canonical foundation collection mappings. | ADR 0021, decision map, canonical data catalog. | `dense-data-os.test.ts`, domain surface registry guard. | Complete first-wave packs beyond graduated centers; keep tightening no-duplicate audits as more packs graduate. |
| DD-002 | The goal is structure of data plus CLI, not UI. | Implemented for first slices: dense routes are CLI-only and use shared DB paths. | CLI docs and ADR 0021. | `cli-discovery.test.ts` covers direct CLI routes. | Extend docs/help for every graduated top-level noun. |
| DD-003 | Domains with specialized software and dense relational data must be handled as a class, not only medicine. | In progress: first-wave systems cover health/EHR, research/CTMS, biology, labs/LIMS, legal, ERP, CRM, finance/accounting, education/LMS, HR/HRIS, manufacturing/MES, ops/ITSM, real estate/proptech, insurance, maintenance/CMMS, procurement/purchasing, warehouse/WMS, supply chain/SCM, transport/TMS, compliance/GRC, government/gov, IoT, construction, ELN, content/CMS, product/PIM/PLM, and pharma/GxP; graduated executable routes now span all first-wave systems with representative canonical records. Roadmap remains visible for energy/utilities, telecom/network ops, hospitality/PMS, agriculture/farm management, nonprofit/grants, media production, aerospace/MRO, banking/core banking, and public safety/CAD. | ADR 0021, dense registry, CLI docs. | `dense-data-os.test.ts` verifies first-wave and roadmap visibility; `cli-discovery.test.ts` covers representative DB-backed routes. | Broaden semantic views, fixtures, and exhaustive intent coverage beyond representative routes. |
| DD-004 | Completeness is measured by expected CLI intentions: `patient list`, save symptoms/medications/labs/encounters, list a patient's medications/labs/encounters, and equivalent professional intents. | In progress: `patient list/create/schema`, `encounter add --patient`, `patient <id> encounter add`, `patient <id> encounters list`, `medication add --patient`, `patient <id> medications list`, `patient <id> symptoms add/list`, `patient <id> lab add`, `patient <id> labs list`, `lab add --patient`, `company/account/deal/product/invoice`, and foundation routes such as `evidence-source create` and `quality-gap create` execute against canonical collections; generated dense intents expose covered routes and workflow gaps. | ADR 0021 and CLI docs. | `cli-discovery.test.ts` covers the graduated routes; `dense-data-os.test.ts` and `inspect-cli.test.ts` cover generated dense intents. | Add fuller per-pack intent fixtures and external-pending examples. |
| DD-005 | `claw patient list` must work without `health` in the command. | Implemented for patient; direct center nouns are supported by dense routing. | CLI docs, ADR 0021. | `cli-discovery.test.ts` asserts `patient create/list` route through `database`. | Add generated coverage for every audited collection singular/plural noun. |
| DD-006 | Do not go case-by-case when the pattern is inferable; apply the same model to 10-20 dense domains. | In progress: common registry and DB routing are generic; first concrete graduation spans all first-wave systems; synthetic acceptance fixtures cover the same pattern across health, research, labs, legal, ERP, ops, education, HR, manufacturing, real estate, insurance, and maintenance. | ADR 0021. | Registry, fixture, inspect, and CLI tests cover cross-domain systems. | Broaden semantic-view execution and real pack-specific fixture depth. |
| DD-007 | The plan must include all three layers: mega-spec/canon, standards/registry thinking, and implementation. | In progress: registry/types/docs/tests exist; first-wave routes and the universal data foundation are executable through the shared database; inspect exposes dense registry, generated intents, semantic views, and acceptance fixtures; `dense-fixtures seed` writes both domain records and registry records into `core.sqlite`, including `domain_systems`, `domain_packs`, `domain_roles`, `domain_profiles`, `canonical_operations`, `semantic_views`, generated `domain_intents`, and `external_pending` quality gaps; `patient <id> timeline`, `study <id> timeline`, `case <id> timeline`, `service <id> timeline`, `sample <id> timeline`, `experiment <id> timeline`, `learner <id> timeline`, `course <id> timeline`, `employee <id> timeline`, `property <id> timeline`, `insurance-policy <id> timeline`, `vehicle <id> timeline`, `purchase-order <id> timeline`, `warehouse <id> timeline`, `supply-plan <id> timeline`, `shipment <id> timeline`, `control <id> timeline`, `public-case <id> timeline`, `product-spec <id> timeline`, `drug-product <id> timeline`, `content-entry <id> timeline`, `thing <id> timeline`, `construction-project <id> timeline`, `lab-notebook <id> timeline`, `company <id> timeline`, `asset <id> timeline`, `work-order <id> timeline`, `erp company <id> overview`, `crm account <id> overview`, and `finance entity <id> overview` materialize semantic views from domain records, relations, evidence, provenance, and quality-gap records. | ADR 0021, decision map, CLI docs, this matrix. | Unit, CLI, docs, inspect, fixture seeding, materialized semantic-view routes, and registry guard validations. | Add materialized views for other first-wave timelines/overviews where packs have enough real data. |
| DD-008 | ERP, legal, and similar systems must be implemented at DB/system level, not just mentioned. | In progress: health has patient encounter, medication, symptom, and lab-result paths plus materialized patient timelines; ERP/CRM have executable company/account/deal/product/invoice/payment paths plus a materialized ERP company overview and direct company timeline across shared company, CRM, billing, product catalog, payment, ops, manufacturing, evidence, provenance, and quality-gap collections; CRM also has a materialized account overview over `accounts`, company anchors, deals, contacts, activities, evidence, provenance, and quality gaps; finance/accounting has financial accounts, transactions, and a materialized finance/accounting entity overview; legal has `case`, legal client, case evidence, evidence/quality-gap attachment, and materialized case timelines; HR has employee, time-off, performance-review, and payroll routes plus materialized employee timelines; real estate has property, visit, offer, and inspection routes plus materialized property timelines; insurance has insurance-policy and vehicle-insurance-policy routes plus materialized policy timelines; maintenance has vehicle, vehicle-maintenance, appliance, and appliance-maintenance routes plus materialized vehicle timelines; procurement has supplier, purchase-order, purchase-order-line-item routes plus materialized purchase-order timelines; warehouse has warehouse, inventory-item, and stock-movement routes plus materialized warehouse timelines; supply chain has supply-plan, supply-plan-item, and supply-risk routes plus materialized supply-plan timelines over procurement and warehouse records; transport/TMS has carrier, shipment, shipment-leg, and freight-rate routes plus materialized shipment timelines over procurement and warehouse records; compliance has control, obligation, control-assessment, and compliance-finding routes plus materialized control timelines; government/gov has agency, public-case, permit, and public-filing routes plus materialized public-case timelines without colliding with legal cases; product/PIM/PLM has product-spec, product-revision, product-requirement, and product-bom routes plus materialized product-spec timelines without replacing `products_catalog`; pharma/GxP has drug-product, batch-record, lot-release, and adverse-event routes plus materialized drug-product timelines over catalog products, product specs, companies, patients/studies, evidence, provenance, and quality gaps without replacing ERP `product`; content/CMS has content-brand, content-destination, content-campaign, content-entry, content-revision, content-variant, content-approval, and content-publication routes plus materialized content-entry timelines without restoring legacy `posts`/`campaigns`/`publications` shortcuts or taking over CRM `campaign`; IoT has thing, device, sensor-reading, and device-command routes plus materialized thing timelines over existing iot config/serve boundaries; construction has construction-project, site, RFI, and change-order routes plus materialized construction-project timelines; ELN has lab-notebook, notebook-entry, protocol-run, and experiment-observation routes plus materialized lab-notebook timelines over research/biology/labs records; ops has `service`, incidents linked to services, evidence/quality-gap attachment, and materialized service timelines; research has studies/participants; labs has samples/assays; education has learners/courses connected through `entity_relations` plus materialized learner and course timelines; manufacturing has assets, asset-linked work orders, materialized asset timelines, and materialized work-order timelines; biology has organisms and biology experiments linked to samples. | ADR 0021, CLI docs. | Cross-domain route coverage in `cli-discovery.test.ts`; acceptance fixture coverage; built-in registry and smoke tests pass. | Add richer semantic views and no-duplicate audit gates. |
| DD-009 | No parallel system: review current notes, graph, knowledge, associations, signals, attachments, custom fields, CRM, billing, ERP, infra/ops before closing. | In progress: current work reuses built-in collections, `core.sqlite`, aliases, DB CRUD, and surface registry rather than a new store; the existing `entity_relations` table is generalized as the universal relation graph instead of adding a second graph; existing surfaces have an explicit integration audit mirrored in `clawDenseDataOsRegistry.existingSurfaceIntegrations` with disposition, canonical owner, shared primitives, dense-system references, and follow-up gate. | Goal file, ADR 0021, decision map, existing catalog audit. | Built-in registry/smoke tests plus domain surface guard, rendered docs link check, `dense-data-os.test.ts`, and `scripts/verify-dense-data-goal.mjs` coverage for every required existing-surface audit row and structured integration registry entry. | Add retire/merge gates for newly discovered duplicated surfaces as more of the pre-v1 reset is audited. |
| DD-010 | Data can be partial, but gaps must be explicit and not confused with bugs. | In progress: dense intent statuses include `data_gap`, `workflow_gap`, `external_pending`, `blocked`; ungraduated dense routes return structured gaps; `quality_gaps`, `evidence_sources`, and `provenance_events` are first-class collections; acceptance fixture includes a partial-data quality gap backed by evidence/provenance; registry records concrete `external_pending` requirements for EHR/FHIR, lab instruments, CTMS sync, payment settlement, live monitor/APM ingestion, pharma regulated exports, and external content publication delivery. | ADR 0021. | `dense-data-os.test.ts`, `cli-discovery.test.ts`, and `inspect-cli.test.ts` cover health gaps, quality-gap/evidence-source routes, dense fixtures, and external-pending registry rows. | Broaden external-pending rows as new provider packs graduate. |
| DD-011 | The final goal must not close until every decision Q/A is reviewed one by one. | In progress: this matrix is seeded from the source user decision messages and guarded by the dense goal verifier. | This document and the source decision audit. | Manual source-session extraction performed during implementation; `scripts/verify-dense-data-goal.mjs` checks DQ-001..DQ-018 and DD-001..DD-011 are present. | Before completion, re-read the full source session and mark every row complete or blocked with evidence. |
