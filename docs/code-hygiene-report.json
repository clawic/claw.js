{
  "schemaVersion": 1,
  "program": "code-hygiene",
  "generatedAt": "2026-05-17",
  "status": "active",
  "blockingFindings": 0,
  "reportOnlyFindings": 24,
  "baselinedFindings": 5,
  "lastAuditSummary": {
    "scannedFiles": 6972,
    "todoFindings": 0,
    "duplicateAssetGroups": 24,
    "duplicateAssetFiles": 65,
    "unreferencedAssetCandidates": 0
  },
  "knipSummary": {
    "filesWithIssues": 58,
    "totalIssues": 567,
    "issueTypes": {
      "unlisted": 2,
      "exports": 509,
      "owners": 55,
      "duplicates": 1
    }
  },
  "peripherySummary": {
    "status": "external-pending",
    "packageCount": 2,
    "scannedPackages": 0,
    "failedPackages": 0,
    "totalFindings": 0
  },
  "externalPending": [
    {
      "id": "periphery-binary-unavailable",
      "tool": "periphery",
      "message": "Periphery 3.7.4 is pinned and report-only, but the binary is not installed on PATH for the calibration scan."
    }
  ],
  "notes": [
    "Initial cleanup completed: blocking findings are zero, actionable TODO/FIXME/HACK/XXX findings are zero, and unreferenced asset candidates are zero after worktree/generated/public asset calibration and removal of the iOS all-projects placeholder.",
    "Use scripts/code-hygiene-audit.mjs for report-only TODO, duplicate asset, and unreferenced asset candidate findings.",
    "Use scripts/code-hygiene-periphery.mjs for report-only Swift package calibration once the pinned binary is available.",
    "Initial ClawJS dependency cleanup removed chokidar from sessions and @noble/hashes from profile.",
    "Knip entrypoint calibration now includes tool configs, local scripts, seed files, and public JS assets.",
    "ClawJS built-in family barrel findings plus two reviewed dynamic-runtime findings are covered by expiring baseline entries.",
    "Root dependency normalization moved VitePress/Vue ownership to website, declared direct root tsx usage, and calibrated the Vitest coverage provider dependency.",
    "Knip workspace calibration now maps top-level app/module workspaces to their own package manifests with explicit entry/project shapes, drops reviewed direct app/database dependencies now provided by actual imports, and baselines two reviewed dynamic runtime findings.",
    "Workspace dependency cleanup removed reviewed unused direct dependencies from top-level app/module package manifests, updated their package locks, expanded report-only Knip entry ownership for memory/modules service entrypoints, and reduced dependency/devDependency findings to zero.",
    "Discord test catalog cleanup made file-local operation field constants private while preserving the test helpers imported across Discord request/executor tests.",
    "Secrets crypto cleanup made module-private constants and helper functions private while preserving the server crypto API used by stores, resolver, app, scripts, and smoke tests.",
    "Knip calibration now treats templates, public CSS, website CSS/HTML, generated fixtures, and reviewed cross-repo e2e imports as report-only owned surfaces.",
    "Unused file cleanup removed reviewed legacy CLI sidecar command files, private server re-export wrappers, placeholder clients, and unreferenced configs; Knip file findings are now zero.",
    "Dead-file cleanup removed reviewed one-line server stubs, retired CLI bin shims, stale generated configs, and unreferenced helper modules while keeping package surface bins explicitly allowlisted.",
    "Database store-helper cleanup made clearly internal parser, normalizer, regex, and constraint helpers private while keeping the database package contract unchanged.",
    "V1 data core cleanup made session indexing, sidecar schema, TOML rendering, SQL identifier, and text utility helpers private while preserving the v1 data package re-export contract.",
    "Slides cleanup made manifest helper types, layout/theme constants, and local validation helpers private while preserving the `claw slides` command contract.",
    "Database magic and style schema cleanup removed retired productivity alias helpers and made style-only structural types private while preserving CLI style/template behavior.",
    "Agent plan cleanup made local risk/policy evaluation helpers private while keeping the plan CLI and delegation graph test surface unchanged.",
    "Time logic cleanup made local temporal helper aliases and parsing helpers private while keeping exported temporal input/status contracts unchanged.",
    "Execution and Relay protocol cleanup made nested payload/envelope helpers private where only the union or public request/response envelopes are externally consumed.",
    "Secrets shared type cleanup kept exported only the role/principal types imported by auth and made the unused local DTO descriptors private.",
    "Execution shared type cleanup made local status aliases and unused helper DTOs private while preserving imported records and protocol types.",
    "IoT tool registry cleanup made local descriptor and invocation helpers private while keeping server route registration exported.",
    "Publishing schema cleanup made nested Zod schema helpers private and removed unused inferred type aliases while preserving route-imported validators.",
    "Apps store cleanup made local record/input helpers private and removed an unused root-dir helper while preserving `createAppsStore`.",
    "Runtime plugin bridge cleanup made low-level OpenClaw plugin command helpers private while preserving the public bridge facades.",
    "CLI runtime metadata cleanup made actor/guidance helper exports private while preserving the CLI metadata provider entrypoint.",
    "Project scaffold cleanup made local project file, package JSON, config writer, config factory, and resource-entry helpers private while preserving CLI project generate/add/info flows.",
    "Chat CLI cleanup made local context and exit-code helpers private while preserving chat/provider CLI entrypoints.",
    "Report governance cleanup made local budget, search, similarity, and age parser helpers private while preserving exported governance functions.",
    "Audio catalog cleanup renamed the local legacy import helper to catalog terminology and removed legacy transcript provider metadata.",
    "Memory CLI cleanup made local exit-code and usage helpers private while preserving the `runMemoryCli` entrypoint.",
    "References storage cleanup made local path and Markdown serialization helpers private while preserving generated reference IDs and CLI storage flows.",
    "Secrets plugin type cleanup made nested field, governance, context, action, resource, and sync helper interfaces private while preserving plugin manifest contracts.",
    "Bridge platform cleanup removed unused platform predicate and TCC capability helper exports while preserving `detectPlatform`.",
    "Template surface cleanup stopped re-exporting unused builtin counters and CLI option types, and removed unused render DTO aliases while preserving `claw template` behavior.",
    "Private shared-type cleanup made unused publishing/content/drive/ERP helper aliases private or removed them while preserving imported record contracts.",
    "V1 data cleanup removed duplicate exit-code/schema re-exports from the CLI wrapper while preserving the consumed data-root/storage helpers.",
    "Integration runtime cleanup kept catalog helper metadata public through the package entrypoint and made provider-local Slack/GitHub/Stripe/Airtable helper constants private.",
    "CLI JSON cleanup made unused low-level writer wrappers private and removed a dead error writer alias while preserving command JSON envelopes.",
    "Style surface cleanup stopped re-exporting unused builtin manifest and CLI option types while preserving `claw style` behavior.",
    "Publishing channel contract cleanup made adapter lifecycle helper result types private while preserving the exported adapter interface.",
    "Bridge server cleanup removed private-package duplicate bridge-frame re-exports and made SSH runtime options private.",
    "Bridge job-handler cleanup made internal Zod schemas, inferred request types, logger defaults, JSON-RPC helper types, and command abort errors private while preserving bridge runtime behavior.",
    "Memory cleanup made workspace path, scalar/type-kind/frontmatter aliases, and server option/result helper types private while preserving the dynamically loaded `startMemoryServer` entrypoint.",
    "ERP cleanup removed unused frontend contract list/filter helper interfaces and made form-field, event-type, SQL table, and rewrite helpers private inside the private ERP module.",
    "Integrations provider cleanup made provider-local runtime/source operation aliases private, removed redundant runner type re-exports, and kept public connector and Telegram event contracts available through the package entrypoint.",
    "Publishing cleanup made CLI parser/token helpers, default port, prefixed database class, AppServices, and raw ULID helper private while preserving exported command context, database alias, and prefixed ID APIs.",
    "Secrets cleanup made resolver field/output DTOs and actor helpers private, removed an unused governance re-export, and deleted the unreferenced session cache store class while keeping schema/backup compatibility.",
    "Wiki cleanup made admin JWT claim helpers private and removed unused token/access-policy helper surfaces while preserving auth principal and wiki operation contracts.",
    "Secrets server cleanup made remaining local app/backup/database helpers private, removed stale capability/governance helpers, and deleted the unused session-cache row DTO after the store removal.",
    "Notify cleanup made admin JWT claims, subscription matching, schema surface node construction, and local delivery-state alias private while preserving exported notification contracts through the core surface.",
    "IoT adapter cleanup made provider id constants and local discovery helper types private while preserving adapter classes and runtime connector behavior.",
    "Monitor/execution cleanup made local monitor mode/schema helpers and password-hash implementation helpers private while preserving exported config, schema SQL, and verification APIs.",
    "Drive cleanup narrowed the CLI test barrel to the used client API and made auth/realtime helper types private while preserving SDK, server, and realtime behavior.",
    "Feed cleanup replaced the stale missing-test script with a real auth smoke test, made admin JWT claim helpers private, and removed an unused opaque-token helper.",
    "Content/database/execution cleanup made local form-field and worker option types private and narrowed the database CLI barrel while preserving service and worker behavior.",
    "ClawJS node cleanup made implementation-only runtime, OpenClaw, secrets, Telegram, rules, demo, and schema surface helpers private while preserving the package entrypoint and generated declaration build.",
    "Claw CLI cleanup removed or privatized implementation-only CLI/domain/open/reference/style/template/data helpers while preserving the published CLI entrypoint exports and generated declaration build.",
    "Workspace/delegation/runtime cleanup made local SQLite and provider helpers private and removed the unused command delegation adapter while preserving deterministic delegation behavior.",
    "Drive/browser/relay/plugin cleanup made local error/location/envelope helpers private and removed unused browser audit and plugin session-key wrappers.",
    "Bridge/Relay follow-up made the screenshot region type private, wired Relay mDNS cleanup into server shutdown, and fixed bridge pairing to send the current platform contract.",
    "Bridge Iroh cleanup made the remote endpoint descriptor private while preserving the exported node and stream bridge contract.",
    "ClawJS node state cleanup removed the unused Telegram state filename alias while preserving the public Telegram state path resolver.",
    "Built-in catalog type cleanup made field-kind and collection-rule helper aliases private while preserving exported catalog definition interfaces.",
    "Session title cleanup replaced the legacy title alias with a compatibility wrapper while preserving both title-generation entrypoints.",
    "Package default-export cleanup replaced duplicate aliases in search MCP and ESLint config packages with compatibility-preserving default surfaces.",
    "Session stream cleanup replaced OpenClaw compatibility aliases with wrappers while preserving Runtime and OpenClaw streaming entrypoints.",
    "Memory server dynamic entrypoint is baselined with expiry because the Claw CLI launcher imports startMemoryServer dynamically across bundled, packed, and development layouts.",
    "Stable surface builder alias is baselined with expiry because ADR 0004 documents clawStableSurface as a public stable-contract alias for persistent surface builders."
  ]
}
