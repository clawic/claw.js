# Remote Gateway And Sync Decision Matrix

Source conversation: `019e36a3-c2e6-73b3-a3fe-f3e7340e42c8`

Reference plan item: `019e3732-c90e-7491-9217-37020c43217e-plan`

This matrix is the public acceptance ledger for the remote Gateway, Connector,
Coordinator, and Sync redesign. It is guarded by
`scripts/verify-remote-sync-goal.mjs` and links to the privacy-safe
[source decision audit](./remote-gateway-sync-source-decision-audit.md).
Before the maintainer-local goal closes, every row must be re-read against the
private source session and marked implemented, documented, or explicitly
blocked.

| ID | Decision key | Implementation state | Canon/docs | Tests/evidence | Remaining work |
| --- | --- | --- | --- | --- | --- |
| RG-001 | `relay_boundary` | Implemented baseline: Relay is split into Coordinator, Gateway, Connector, and Sync nodes and docs. | ADR 0022, Relay docs, decision map, surface registry. | `surface-route-graph-guard`, `inspect-cli.test.ts`, `verify-remote-sync-goal`. | Replace remaining compatibility Relay paths only after clients migrate. |
| RG-002 | `server_trust_model` | Implemented contract: `sovereign_e2e_tunnel` and `governed_gateway` are typed trust modes; `claw nodes trust --record true` stores signed `NodeTrustDecision` records without auto-granting physical authority. | ADR 0022, remote-sync contracts, `RemoteSyncStateStore`. | Core contract tests, remote conformance smoke, and inspect CLI smoke for signed node-trust records. | Physical device trust acceptance remains EXTERNAL PENDING. |
| RG-003 | `remote_surface_parity` | Implemented baseline states plus signed evidence: every registered node with human/programmatic surfaces must classify Relay exposure as direct `remote-safe` or an explicit `local-only`/`blocked` gap; `RemoteSurfaceClassificationReceipt` represents signed per-capability evidence and `remote-safe` requires route, policy, and test refs. | Interface matrix, surface contract registry, ADR 0022, Relay docs, CLI docs, `RemoteSyncStateStore`. | `docs-surface-check`, `claw remote classify`, inspect CLI signed receipt smoke, Relay `/v1/remote/classifications/receipts`, executable `claw inspect remote`, and goal verifier fail-closed registry/receipt/no-pending checks. | Reintroduce `pending` only as a temporary development state; final goal closure requires zero `pending` Relay classifications. |
| RG-004 | `topology_priority` | Implemented baseline for personal mesh plus headless/server topology. | ADR 0022, route graph, CLI docs. | Required nodes/routes in graph guard and goal verifier. | Physical multi-device validation is EXTERNAL PENDING until real nodes are paired. |
| RG-005 | `sync_authority_model` | Implemented manifest contract with authority, residency, owner, allowed peers, routes, and driver; `claw sync manifest --state-dir &lt;dir&gt; --record true` persists the local manifest ledger, and `SyncAuthorityHandoffReceipt` records signed source-node, target-node, requested-authority, and requested-residency handoff intent without opening trust mutations. | `SyncResourceManifest`, `SyncAuthorityHandoffReceipt`, ADR 0022, `RemoteSyncStateStore`, Relay docs, CLI docs. | Core sync authority receipt tests, CLI `sync manifest`, inspect CLI signed `claw sync handoff --record true` smoke, Relay `/v1/sync/authority-handoffs` test, and goal verifier. | Physical cross-device authority handoff remains signed-host/Coordinator gated as `physical_authority_handoff` `EXTERNAL PENDING`. |
| RG-006 | `remote_secrets_model` | Implemented contract and local signed broker/provider operation: secret refs only, broker lease required, plaintext forbidden, `claw gateway secret-lease` stores signed expiring lease metadata, and `claw gateway secret-provider` stores signed provider receipts without reading or returning secret values. | ADR 0022, remote-sync contracts, interface matrix, `RemoteSyncStateStore`. | Core tests validate secret policy plus `RemoteSecretLease`; inspect CLI smoke validates signed lease issuance, provider receipt, redaction, and explicit provider pending flags. | Physical provider secret retrieval remains signed-host/provider gated. |
| RG-007 | `transport_contract` | Implemented local signed contract: Iroh v1 is represented as adapter, contract stays transport-agnostic, and `claw nodes heartbeat --record true` stores a signed `RemoteTransportHandshakeReceipt`. | ADR 0022, surface graph, remote-sync contracts, `RemoteSyncStateStore`. | Remote conformance reports `transport_agnostic_iroh_v1_adapter`; inspect CLI smoke validates signed heartbeat receipt and explicit physical pending flags. | Physical Iroh handshake and device trust acceptance remain EXTERNAL PENDING. |
| RG-008 | `remote_api_shape` | Implemented baseline plus route contract catalog: Gateway routes broker local registered contracts instead of a parallel API, with CLI and HTTP service routes sharing the same core contracts; every required route has local refs, remote entrypoints, parity required, and `parallelApiAllowed: false`. | ADR 0022, Relay docs, CLI docs, decision map, surface route graph, `RemoteRouteContractCatalog`. | Route graph guard, CLI/router parity, `claw remote contracts`, Relay `/v1/remote/route-contracts`, remote-sync route tests, and goal verifier. | Expand per-domain remote-safe conformance as domains close. |
| RG-009 | `offline_behavior` | Implemented executable planner plus no-write queue/reconciliation contract: interactive remote commands fail fast and are not enqueued; sync push/pull changes queue, conflicts block, cursors advance only after acknowledgement or explicit resolution; `claw sync run --state-dir &lt;dir&gt; --queue true` persists that local queue and `claw sync reconcile` updates it, optionally with Coordinator signatures. | CLI docs, Relay docs, ADR 0022, `remote-sync.ts`, and `remote-sync-state-store.ts`. | Core tests, inspect CLI tests, `claw sync plan`, `claw sync run --queue true`, signed ledger smoke, Relay `/v1/sync/plan`, and `verify-remote-sync-goal`. | Physical/provider sync application remains signed-host/Coordinator gated. |
| RG-010 | `remote_actor_model` | Implemented actor context, fail-closed access evaluation, and signed local Gateway audit receipts for human, device, agent, service, and organization actors; agent requests require assignment and emit audit decisions bound to `hostAuditStore: signed_host_audit`. | ADR 0022, Relay docs, CLI docs, remote-sync contracts, `RemoteSyncStateStore`. | Core schema/access tests, inspect CLI signed `gateway audit --record true` smoke, Relay `/v1/gateway/audit/receipts` test, and goal verifier. | Physical signed host audit-store persistence remains EXTERNAL PENDING. |
| RG-011 | `headless_host_model` | Implemented baseline route and node for complete headless hosts. | ADR 0022, surface registry, CLI docs. | Required route `gateway.headlessAgentHost`. | Real daemon/server deployment validation is EXTERNAL PENDING. |
| RG-012 | `first_vertical_slice` | Implemented baseline covers chat, search, secret broker, sync, headless agents, and multi-tenant service through required route contracts; `RemoteProviderDeviceE2EValidationPlan` binds those domains into one final no-write E2E gate before the provider/device row can clear. | ADR 0022, route graph, `RemoteRouteContractCatalog`, `RemoteProviderDeviceE2EValidationPlan`, external validation checklist, external validation evidence template, external validation report. | Required route checks in core tests, `claw remote contracts`, `claw remote e2e-plan`, `claw remote validation-checklist`, `claw remote validation-template`, `claw remote validation-report`, Relay `/v1/remote/route-contracts`, Relay `/v1/remote/provider-device-e2e-plan`, Relay `/v1/remote/external-validation-checklist`, Relay `/v1/remote/external-validation-template`, Relay `/v1/remote/external-validation-report`, provider/device E2E plan checks, and goal verifier. | End-to-end physical integration remains per-provider EXTERNAL PENDING. |
| RG-013 | `sync_substrate` | Implemented driver taxonomy plus signed local driver-application receipts for skills, memory, sessions, drive/files, blobs, SQLite, partial SQLite, sidecars, search index, agent config, and workspace state. | ADR 0022, Relay docs, CLI docs, `syncDriverSchema`, `SyncDriverApplicationReceipt`, `RemoteSyncStateStore`. | Core route-driver/application receipt tests, inspect CLI signed `sync apply --record true` smoke, Relay `/v1/sync/applications` test, and goal verifier. | Physical driver execution remains signed-host/driver `EXTERNAL PENDING`. |
| RG-014 | `conflict_default` | Implemented: diverged snapshots create open conflicts and never silently overwrite. | ADR 0022, CLI docs. | Core and CLI tests for conflict/noop/push planning. | Add human review UI later; the contract already blocks silent overwrite. |
| RG-015 | `client_cache_policy` | Implemented strict encrypted TTL cache policy plus signed client cache metadata snapshots with no plaintext, no secrets, and no authoritative state. | ADR 0022, manifest schema, `RemoteClientCacheSnapshot`, `RemoteSyncStateStore`. | Core manifest tests and inspect CLI smoke for `claw sync cache --record true`. | Physical client storage implementation remains host/client gated. |
| RG-016 | `guardrail_strictness` | Implemented baseline conformance, fail-closed access evaluation, no-pending Relay classification guard, and consolidated external-pending audit: missing required nodes/routes fail the report, missing grants/classification/trust deny the request, physical/provider/deployment blockers are listed by `RemoteExternalPendingRegister`, the external validation checklist binds every blocker to approved commands/artifacts/criteria, the external validation evidence template gives operators a no-write JSON skeleton for each blocker, the external validation report refuses to clear incomplete, plaintext, or approval-unreferenced evidence, the source Q/A review template gives operators a no-write skeleton for the 23 source rows, the source Q/A review report requires disposition and evidence refs for every Q/A row, the closure gate refuses final close without all 23 source Q/A rows and clearable external evidence, and `claw inspect remote` exposes classification, Sync authority/drivers, transport, route contracts, tests, gaps, validation checklist/template/report, source Q/A template, closure gate, and conformance together. | ADR 0022, Relay docs, CLI docs, decision map, goal verifier. | `remote conformance`, `claw remote pending`, `claw remote validation-checklist`, `claw remote validation-template`, `claw remote validation-report`, `claw remote source-qa-template`, `claw remote closure-gate`, `claw inspect remote`, Relay `/v1/remote/external-pending`, Relay `/v1/remote/external-validation-checklist`, Relay `/v1/remote/external-validation-template`, Relay `/v1/remote/external-validation-report`, Relay `/v1/remote/source-qa-template`, Relay `/v1/remote/closure-gate`, graph guard, core access tests, inspect CLI tests, goal verifier. | Clear external-pending rows only after approved physical/provider validation, `approvedRunRef`, and final source Q/A review. |
| RG-017 | `compat_policy` | Implemented adapter contract: compatibility Relay/mobile routes stay only as `RemoteCompatibilityAdapterReceipt` records that map legacy surfaces to canonical routes and forbid parallel APIs. | ADR 0022, Relay docs, CLI docs, `remote-sync.ts`, `RemoteSyncStateStore`. | Required Relay and Gateway route tests, inspect CLI signed `claw remote compat` smoke, Relay `/v1/remote/compatibility/adapters` tests, and goal verifier. | Retire compatibility names only through a migration ADR. |
| RG-018 | `hosted_service_position` | Implemented signed local deployment projection: hosted/self-hosted parity is required by conformance, and `claw gateway serve|project --record true` stores `GatewayDeploymentManifest` records with the same route contract for both deployment kinds. | ADR 0022, CLI `gateway conformance`, CLI docs, Relay docs, `remote-sync.ts`, `RemoteSyncStateStore`. | Inspect CLI tests, signed Gateway deployment smoke, and goal verifier. | Real self-hosted process binding and hosted rollout validation remain EXTERNAL PENDING. |
| RG-019 | `layer_names` | Implemented canonical taxonomy: Coordinator, Gateway, Connector, Sync. | ADR 0022, docs, CLI, registry. | Docs checks and goal verifier. | Keep Relay naming compatibility-only. |
| RG-020 | `mesh_collaboration_scope` | Implemented no-write inter-mesh contracts for `MeshInvitation`, `MeshInvitationAcceptance`, `MeshResourceShare`, and `MeshRevocation`; invitations scope resources/actions, acceptances record signed intent, shares bind to Sync manifests, and revocations cascade Sync queue access. | ADR 0022, route graph, CLI docs, Relay docs, `remote-sync.ts`, `remote-sync-state-store.ts`. | Core tests, Relay route tests, CLI smokes for `nodes invite|accept|share|revoke|trust`, route graph guard, durable state smoke, signed trust smoke, and goal verifier. | Physical peer trust and device acceptance remain Coordinator/physical-device `EXTERNAL PENDING`. |
| RG-021 | `agent_service_model` | Implemented signed local Gateway service receipt: tenant, agent, assignment, route, budget, billing account, isolation key, audit requirement, runtime pending state, and billing meter pending state must all be explicit. | ADR 0022, Relay docs, CLI docs, route graph, `remote-sync.ts`, `RemoteSyncStateStore`. | Core tests, Relay route tests, CLI signed `gateway agent-service --record true` smoke, and `verify-remote-sync-goal`. | Real runtime execution and real billing meter persistence remain signed-host/Coordinator `EXTERNAL PENDING`. |
| RG-022 | `sync_lateral_domains` | Implemented lateral sync taxonomy and explicit route mapping across skills, memory/user-model, sessions, drive/files, blobs, search indexes, DBs/partial DBs, sidecars, agent config, and workspace state. Sessions, blobs, sidecars, search indexes, agent config, and workspace state now have Sync-plane route contracts instead of relying on Gateway or SQLite aggregate routes as storage. | ADR 0022, route graph, `RemoteRouteContractCatalog`, remote-sync contracts. | Core driver mapping tests, route contract catalog tests, `claw inspect remote`, and goal verifier. | Add physical drivers without changing the governance manifest. |
