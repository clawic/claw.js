# Dense Data Completion Audit

Source conversation: `019e35a1-06bb-77f2-a712-92ed2646bd15`

Reference plan item: `019e3659-0335-7811-9cda-c9d176e91515-plan`

This audit is the public acceptance gate for the dense data operating system.
It is intentionally stricter than the decision matrix: a row may stay
`in_progress` while the goal remains active, but no final close is allowed
until every row is either `implemented` with evidence or `external_pending`
with a concrete provider/physical/regulatory blocker.
It tracks top-level human CLI nouns, materialized timelines/overviews,
`core.sqlite` storage, no-parallel-system reuse, and explicit
`EXTERNAL PENDING` blockers.
The checklist keeps top-level human CLI nouns, materialized timelines/overviews,
and no-parallel-system evidence visible as explicit close criteria.

Private source-session paths and local goal paths are intentionally excluded
from this public file. The private goal requires the final agent to re-read the
source session and update the source decision audit before completion.

| ID | Acceptance requirement | Current status | Evidence | Remaining blocker before final close |
| --- | --- | --- | --- | --- |
| GA-001 | Full source session read and every decision-bearing user answer reviewed one by one. | implemented | [Dense Data Source Decision Audit](./source-audit.md) enumerates DQ-001..DQ-018; the source JSONL was re-read before finalization, duplicate transport/user-context turns were collapsed, and every decision row now has concrete implementation/process evidence. `scripts/verify-dense-data-goal.mjs` guards the row set. | Future final-close work must repeat the source-session check if new decision turns appear. |
| GA-002 | Decision matrix maps decision -> implementation -> docs/canon -> tests -> evidence/blocker. | implemented | [Dense Data Decision Matrix](./decision-matrix.md) maps DD-001..DD-011 to implementation, canon, tests, and ongoing guards; `scripts/verify-dense-data-goal.mjs` verifies all required rows remain present. | Future decisions must update the matrix and verifier together. |
| GA-003 | No old competing catalog, graph, evidence model, CRM, billing, ERP, or ops system remains active without integration or retirement. | implemented | [Dense Data Existing Catalog Audit](./existing-catalog-audit.md), `clawProfessionalRecordsOsRegistry.existingSurfaceIntegrations`, `entity_relations` as universal operational relation graph, shared `people`/`domain_profiles`, and verifier checks for existing-surface rows prove the audited surfaces are reused, split by purpose, or guarded rather than duplicated. | Newly discovered duplicate surfaces during future reset work must be integrated, retired, replaced, or added as explicit blockers. |
| GA-004 | `core.sqlite` is the primary store for user-facing structured dense records. | implemented | Foundation mappings, dense direct CLI routes, `dense-fixtures seed`, and materialized semantic views route through the main DB/store and document `core.sqlite`. | Sidecar exceptions must remain limited to blobs, search indexes, secrets, high-churn runtime state, or other documented technical reasons. |
| GA-005 | Top-level human CLI nouns exist for audited dense collections without requiring a domain prefix. | implemented | Generated dense intents cover every graduated center noun and alias; `scripts/verify-dense-data-goal.mjs` checks every center collection has covered `list/get/create/update/delete/query/schema` intents, and `cli-discovery.test.ts` executes every graduated noun and alias as a top-level `list` route through either shared `database` CRUD or an executable dense semantic view such as `invoice list`. | New graduated centers must keep the generated top-level route smoke green or be recorded as workflow/data gaps before close. |
| GA-006 | Recognizable professional areas are visible while sharing engines. | implemented | Dense systems registry exposes first-wave systems plus roadmap systems; shared engines include identity/profile, evidence/provenance, quality gaps, relation graph, semantic views, intent coverage, vocabulary/units, instruments, timelines, workflow, finance, and location. | Roadmap systems remain visible taxonomy until graduated; they must not be counted as fully implemented packs. |
| GA-007 | Multiple reasonable CLI routes map to one canonical operation. | implemented | Health, legal, HR, real estate, procurement, warehouse, supply chain, transport, compliance, government, product, pharma, content, IoT, construction, and ELN operations declare alternate routes and nested/direct forms; `dense-data-os.test.ts` and `scripts/verify-dense-data-goal.mjs` resolve every multi-route operation route back to the same canonical `operationId`. `cli-discovery.test.ts` now executes high-value dense-data alternate routes over fixture records and verifies each route writes or reads the canonical collection/relation shape, including nested/direct pairs for patient encounter/medication/lab, legal client, HR time off, property visit/offer, maintenance, procurement, warehouse, supply chain, transport, compliance, government, construction, IoT, ELN, content, product, pharma, and finance/accounting overview aliases. | New multi-route operations must add resolver convergence plus executable alternate-route smoke before final close. |
| GA-008 | Partial data is allowed and explicit gaps are queryable. | implemented | `quality_gaps`, `evidence_sources`, `provenance_events`, dense gap registry, `claw inspect dense-gaps`, materialized views with `partial`, and fixture coverage for partial-data gaps. | New provider/physical/regulatory packs must add explicit `external_pending` or quality-gap rows as they graduate. |
| GA-009 | Minimal shared identity plus roles/profiles prevents duplicated people/entities. | implemented | Acceptance fixtures now include `people` base identities, `same_as` `entity_relations` from shared people to patient, participant, learner, employee, and legal-client records, generated `domain_profiles` for patient, participant, legal client, learner, employee, organization, and CRM account roles, plus CLI fixture smoke that reads the identity relations and profile records after `claw dense-fixtures seed`. | New identity-bearing centers must link to `people`, `domain_profiles`, or explicit no-duplicate relation gaps before close. |
| GA-010 | Real orchestrators exist for dense domains, not empty portals. | implemented | First-wave systems declare centers, operations, semantic views, shared engines, standards, DB-backed routes, fixture records, generated intent coverage, materialized views, and high-value alternate-route execution. ERP/CRM/finance/ops plus health, research, legal, labs, education, manufacturing, supply-chain, content, product, pharma, IoT, construction, and ELN all have executable local orchestration evidence. | Provider/live-service orchestration remains separated as `EXTERNAL PENDING`. |
| GA-011 | Required first wave is implemented: health/EHR, research/CTMS, biology, labs/LIMS, legal, ERP, CRM, finance/accounting, education/LMS, manufacturing/MES, ops/ITSM. | implemented | Registry and verifier require the listed systems plus expanded first-wave domains; `cli-discovery.test.ts` covers DB-backed routes, semantic views, generated top-level nouns, fixtures, and high-value alternates; `scripts/verify-dense-data-goal.mjs` confirms 27 first-wave systems. | Roadmap packs remain visible contracts/gaps until separately graduated. |
| GA-012 | Existing catalog is aggressively reaudited under pre-v1 reset rules. | implemented | Existing catalog audit, structured integration registry, no-parallel-system docs, `dense-data-os.test.ts`, and verifier checks cover notes/pages, knowledge/facts, knowledge graph, associations/custom fields, signals, attachments/files, CRM, billing/finance, ERP, infra/ops, and identity/roles/teams. | Any additional duplicated or stale surfaces found during future reset work must be resolved rather than left as legacy. |
| GA-013 | Guardrails landed before claiming completion. | implemented | `assertClawProfessionalRecordsOsRegistryComplete`, `dense-data-os.test.ts`, `domain-surface-registry.test.ts`, `inspect-cli.test.ts`, `cli-discovery.test.ts`, and `scripts/verify-dense-data-goal.mjs`. | Guardrails must keep expanding with every new pack graduation. |
| GA-014 | Inspect/discovery exposes DB, catalog, systems, intents, gaps, semantic views, fixtures, and existing-surface integration coherently. | implemented | `inspect-cli.test.ts` directly smokes `claw inspect dense-data`, `dense-gaps`, `dense-intents`, `dense-views`, and `dense-fixtures`; assertions cover foundation DB mappings, systems, external-pending rows, existing-surface integrations, generated intents, semantic views, and fixture records. `cli-discovery.test.ts` covers `dense-fixtures seed` against `core.sqlite`. | New inspect surfaces or pack-specific discovery routes must add direct smoke before final close. |
| GA-015 | Semantic views are materialized where the pack has enough local data. | implemented | Materialized timelines/overviews cover patient, patient medications, study cohort, study, sample, experiment, case evidence, case, service, learner, course, employee, property, insurance policy, vehicle, purchase order, warehouse, supply plan, shipment, control, public case, product spec, drug product, content entry, thing, construction project, lab notebook, company, asset, work order, invoice list, ERP company overview, CRM account overview, and finance entity overview. `cli-discovery.test.ts` now proves `claw patient <id> medications list`, `claw study <id> cohort list`, and `claw case <id> evidence list` return materialized semantic views from `core.sqlite` records rather than contract-only gaps. | New semantic views must either materialize from local DB records or be explicitly marked as contract-only/external-pending before final close. |
| GA-016 | Fixtures cover patient, study, sample, legal case, invoice/company, incident/service, course, manufacturing work order, evidence/provenance, and partial-data gaps. | implemented | `clawProfessionalRecordsAcceptanceFixture`, `dense-fixtures seed`, fixture verifier rows, and CLI fixture seed tests. | New pack graduations must extend fixture coverage. |
| GA-017 | EXTERNAL PENDING is separated from bugs for provider, physical, regulated, cost-bearing, and live-service requirements. | implemented | `externalPendingRequirements`, dense gap registry, quality-gap fixture rows, docs using `EXTERNAL PENDING`, and verifier checks for health/labs/research/legal/finance/education/HR/real-estate/insurance/compliance/government/banking/public-safety/ERP/ops/IoT/ELN/pharma/content blockers. The legal closure overlay keeps local organization visible while regulated execution, filings, credit/investment/insurance/employment/admissions/housing/government/public-safety actions remain externally gated. | Live/provider/regulated validation remains outside local proof until explicitly approved and brokered. |
| GA-018 | Final close must prove completion requirement-by-requirement, not by broad green tests. | implemented | This audit, the source decision audit, decision matrix, existing catalog audit, and verifier prove every acceptance row requirement-by-requirement; validation includes core build, typecheck, dense test suite, and `test:dense-data-goal`. | Future goal expansions must add new rows instead of weakening this close gate. |

## Final-Close Rule

This file is the completion checklist for the dense data goal. It keeps the
full goal intact across turns: any future row that re-enters `in_progress`
reopens the close gate until it is implemented or explicitly
`external_pending` with evidence.
