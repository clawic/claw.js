# Dense Data Operating System Decision Matrix

Source conversation: `019e35a1-06bb-77f2-a712-92ed2646bd15`

Reference plan item: `019e3659-0335-7811-9cda-c9d176e91515-plan`

This matrix is the public, privacy-safe acceptance ledger for the dense data
operating system. It must be updated before the goal can close. Private session
paths and local goal files intentionally stay out of the public repository. The
source-turn enumeration lives in
[Dense Data Source Decision Audit](./source-audit.md), and
the final acceptance checklist lives in
[Dense Data Completion Audit](./completion.md).
`scripts/verify-dense-data-goal.mjs` guards this ledger against missing source
decision rows, missing matrix rows, privacy-unsafe public paths, registry drift,
fixture drift, semantic-view drift, missing `external_pending` rows, and missing
existing-surface anti-duplication audit rows.

| ID | User decision | Implementation state | Canon/docs | Tests/evidence | Remaining work |
| --- | --- | --- | --- | --- | --- |
| DD-001 | Build a very ambitious, deeply reasoned data expansion, but avoid chaos, duplicated fields, and meaningless over-modeling. | Implemented: `clawProfessionalRecordsOsRegistry` defines systems, shared engines, centers, operations, semantic views, statuses, roadmap domains, and canonical foundation collection mappings without duplicating shared primitives. | ADR 0021, decision map, canonical data catalog. | `dense-data-os.test.ts`, domain surface registry guard, generated top-level noun smoke, materialized semantic-view tests, and alternate-route smoke. | Ongoing guard: future pack graduations must keep the no-duplicate audit and generated tests green. |
| DD-002 | The goal is structure of data plus CLI, not UI. | Implemented: dense routes are CLI-only and use shared DB paths. | CLI docs and ADR 0021. | `cli-discovery.test.ts` covers direct CLI routes, semantic views, fixtures, and alternates. | Ongoing guard: new graduated nouns must update docs/help and route smoke. |
| DD-003 | Domains with specialized software and dense relational data must be handled as a class, not only medicine. | Implemented: first-wave systems cover health/EHR, research/CTMS, biology, labs/LIMS, legal, ERP, CRM, finance/accounting, education/LMS, HR/HRIS, manufacturing/MES, ops/ITSM, real estate/proptech, insurance, maintenance/CMMS, procurement/purchasing, warehouse/WMS, supply chain/SCM, transport/TMS, compliance/GRC, government/gov, IoT, construction, ELN, content/CMS, product/PIM/PLM, and pharma/GxP; graduated executable routes span all first-wave systems with canonical records, semantic views, fixtures, and route smoke. Roadmap remains visible for energy/utilities, telecom/network ops, hospitality/PMS, agriculture/farm management, nonprofit/grants, media production, aerospace/MRO, banking/core banking, and public safety/CAD. | ADR 0021, dense registry, CLI docs. | `dense-data-os.test.ts` verifies first-wave and roadmap visibility; `cli-discovery.test.ts` covers DB-backed routes, materialized views, generated top-level nouns, and alternates. | Ongoing guard: roadmap packs remain explicit contracts/gaps until separately graduated. |
| DD-004 | Completeness is measured by expected CLI intentions: `patient list`, save symptoms/medications/labs/encounters, list a patient's medications/labs/encounters, and equivalent professional intents. | Implemented: `patient list/create/schema`, `encounter add --patient`, `patient <id> encounter add`, `patient <id> encounters list`, `medication add --patient`, `patient <id> medications list`, `patient <id> symptoms add/list`, `patient <id> lab add`, `patient <id> labs list`, `lab add --patient`, `company/account/deal/product/invoice`, and foundation routes such as `evidence-source create` and `quality-gap create` execute against canonical collections; generated dense intents expose covered routes and workflow gaps; every graduated center noun and alias has an executable top-level `list` smoke through shared database CRUD or an executable dense semantic view; high-value direct/nested alternate routes execute against canonical relation fields instead of only resolving in the registry; `listClawProfessionalRecordsGapRegistryEntries` and `claw inspect dense-gaps` derive an auditable gap registry from partial/workflow/data/external-pending/blocked states. | ADR 0021 and CLI docs. | `cli-discovery.test.ts` covers the graduated routes, generated top-level noun/alias list smoke, and executable alternate-route smoke; `dense-data-os.test.ts`, `inspect-cli.test.ts`, and `scripts/verify-dense-data-goal.mjs` cover generated dense intents and gap registry rows. | Ongoing guard: new pack-specific intents must add fixtures or explicit gap examples as they graduate. |
| DD-005 | `claw patient list` must work without `health` in the command. | Implemented generically: direct center nouns and aliases are supported by dense routing without a domain prefix, so `patient list`, `patients list`, `company list`, `companies list`, and equivalent graduated nouns execute top-level. Semantic list routes such as `invoice list` are accepted only when they expose an executable dense view. | CLI docs, ADR 0021, completion audit. | `cli-discovery.test.ts` asserts `patient create/list`, plural aliases, and a generated smoke over every graduated center noun and alias; `scripts/verify-dense-data-goal.mjs` checks generated intent coverage for every center/action except restricted `purge`. | New graduated centers must keep the generated smoke green or be documented as workflow/data gaps. |
| DD-006 | Do not go case-by-case when the pattern is inferable; apply the same model to 10-20 dense domains. | Implemented: common registry, DB routing, semantic views, fixture seeding, gap handling, and alternate-route testing are generic; first concrete graduation spans all first-wave systems. | ADR 0021. | Registry, fixture, inspect, semantic-view, generated route, and CLI tests cover cross-domain systems. | Ongoing guard: pack-specific depth must continue using the shared model. |
| DD-007 | The plan must include all three layers: mega-spec/canon, standards/registry thinking, and implementation. | Implemented: registry/types/docs/tests exist; first-wave routes and the universal data foundation are executable through the shared database; inspect exposes dense registry, generated intents, semantic views, and acceptance fixtures; `dense-fixtures seed` writes both domain records and registry records into `core.sqlite`, including shared `people` identities, `same_as` `entity_relations`, `domain_systems`, `domain_packs`, `domain_roles`, `domain_profiles`, `canonical_operations`, `semantic_views`, generated `domain_intents`, and `external_pending` quality gaps; `claw patient <id> medications list`, `patient <id> timeline`, `claw study <id> cohort list`, `study <id> timeline`, `claw case <id> evidence list`, `case <id> timeline`, `service <id> timeline`, `sample <id> timeline`, `experiment <id> timeline`, `learner <id> timeline`, `course <id> timeline`, `employee <id> timeline`, `property <id> timeline`, `insurance-policy <id> timeline`, `vehicle <id> timeline`, `purchase-order <id> timeline`, `warehouse <id> timeline`, `supply-plan <id> timeline`, `shipment <id> timeline`, `control <id> timeline`, `public-case <id> timeline`, `product-spec <id> timeline`, `drug-product <id> timeline`, `content-entry <id> timeline`, `thing <id> timeline`, `construction-project <id> timeline`, `lab-notebook <id> timeline`, `company <id> timeline`, `asset <id> timeline`, `work-order <id> timeline`, `invoice list`, `erp company <id> overview`, `crm account <id> overview`, and `finance entity <id> overview` materialize semantic views from domain records, relations, evidence, provenance, and quality-gap records. | ADR 0021, decision map, CLI docs, this matrix. | Unit, CLI, docs, inspect, fixture seeding, materialized semantic-view routes, identity relation smoke, alternate-route smoke, and registry guard validations. | Ongoing guard: new semantic views must materialize locally or declare contract/external-pending status. |
| DD-008 | ERP, legal, and similar systems must be implemented at DB/system level, not just mentioned. | Implemented: health has patient encounter, medication, symptom, and lab-result paths plus materialized patient medication and patient timeline views; ERP/CRM have executable company/account/deal/product/invoice/payment paths plus a materialized ERP company overview and direct company timeline across shared company, CRM, billing, product catalog, payment, ops, manufacturing, evidence, provenance, and quality-gap collections; CRM also has a materialized account overview over `accounts`, company anchors, deals, contacts, activities, evidence, provenance, and quality gaps; finance/accounting has financial accounts, transactions, and a materialized finance/accounting entity overview; legal has `case`, legal client, case evidence, evidence/quality-gap attachment, materialized case evidence, and materialized case timelines; HR has employee, time-off, performance-review, and payroll routes plus materialized employee timelines; real estate has property, visit, offer, and inspection routes plus materialized property timelines; insurance has insurance-policy and vehicle-insurance-policy routes plus materialized policy timelines; maintenance has vehicle, vehicle-maintenance, appliance, and appliance-maintenance routes plus materialized vehicle timelines; procurement has supplier, purchase-order, purchase-order-line-item routes plus materialized purchase-order timelines; warehouse has warehouse, inventory-item, and stock-movement routes plus materialized warehouse timelines; supply chain has supply-plan, supply-plan-item, and supply-risk routes plus materialized supply-plan timelines over procurement and warehouse records; transport/TMS has carrier, shipment, shipment-leg, and freight-rate routes plus materialized shipment timelines over procurement and warehouse records; compliance has control, obligation, control-assessment, and compliance-finding routes plus materialized control timelines; government/gov has agency, public-case, permit, and public-filing routes plus materialized public-case timelines without colliding with legal cases; product/PIM/PLM has product-spec, product-revision, product-requirement, and product-bom routes plus materialized product-spec timelines without replacing `products_catalog`; pharma/GxP has drug-product, batch-record, lot-release, and adverse-event routes plus materialized drug-product timelines over catalog products, product specs, companies, patients/studies, evidence, provenance, and quality gaps without replacing ERP `product`; content/CMS has content-brand, content-destination, content-campaign, content-entry, content-revision, content-variant, content-approval, and content-publication routes plus materialized content-entry timelines without restoring legacy `posts`/`campaigns`/`publications` shortcuts or taking over CRM `campaign`; IoT has thing, device, sensor-reading, and device-command routes plus materialized thing timelines over existing iot config/serve boundaries; construction has construction-project, site, RFI, and change-order routes plus materialized construction-project timelines; ELN has lab-notebook, notebook-entry, protocol-run, and experiment-observation routes plus materialized lab-notebook timelines over research/biology/labs records; ops has `service`, incidents linked to services, evidence/quality-gap attachment, and materialized service timelines; research has studies/participants plus materialized study cohort views; labs has samples/assays; education has learners/courses connected through `entity_relations` plus materialized learner and course timelines; manufacturing has assets, asset-linked work orders, materialized asset timelines, and materialized work-order timelines; biology has organisms and biology experiments linked to samples. | ADR 0021, CLI docs. | Cross-domain route coverage in `cli-discovery.test.ts`; acceptance fixture coverage; built-in registry and smoke tests pass. | Ongoing guard: provider/live integrations stay `EXTERNAL PENDING` until separately validated. |
| DD-009 | No parallel system: review current notes, graph, knowledge, associations, signals, attachments, custom fields, CRM, billing, ERP, infra/ops before closing. | Implemented: current work reuses built-in collections, `core.sqlite`, aliases, DB CRUD, and surface registry rather than a new store; the existing `entity_relations` table is generalized as the universal relation graph instead of adding a second graph; shared identity uses existing `people` plus `domain_profiles` and `same_as` relations instead of a parallel patient/client/learner identity graph; existing surfaces have an explicit integration audit mirrored in `clawProfessionalRecordsOsRegistry.existingSurfaceIntegrations` with disposition, canonical owner, shared primitives, dense-system references, and follow-up gate. | Goal file, ADR 0021, decision map, existing catalog audit. | Built-in registry/smoke tests plus domain surface guard, rendered docs link check, `dense-data-os.test.ts`, `cli-discovery.test.ts`, and `scripts/verify-dense-data-goal.mjs` coverage for every required existing-surface audit row, structured integration registry entry, and shared-identity fixture relation. | Ongoing guard: newly discovered duplicated surfaces must be integrated, retired, replaced, or blocked before future closure claims. |
| DD-010 | Data can be partial, but gaps must be explicit and not confused with bugs. | Implemented: dense intent statuses include `data_gap`, `workflow_gap`, `external_pending`, `blocked`; ungraduated dense routes return structured gaps; `quality_gaps`, `evidence_sources`, and `provenance_events` are first-class collections; acceptance fixture includes a partial-data quality gap backed by evidence/provenance; registry records concrete `external_pending` requirements for EHR/FHIR, lab instruments, CTMS sync, payment settlement, live monitor/APM ingestion, pharma regulated exports, and external content publication delivery; the derived dense gap registry separates intent gaps, external-pending requirements, unknown-intent policy, and restricted purge policy. | ADR 0021. | `dense-data-os.test.ts`, `cli-discovery.test.ts`, `inspect-cli.test.ts`, and `scripts/verify-dense-data-goal.mjs` cover health gaps, quality-gap/evidence-source routes, dense fixtures, external-pending registry rows, and `claw inspect dense-gaps`. | Ongoing guard: new provider packs must add `external_pending` rows as they graduate. |
| DD-011 | The final goal must not close until every decision Q/A is reviewed one by one. | Implemented: this matrix is seeded from the source user decision messages and guarded by the dense goal verifier. | This document and the source decision audit. | Source-session extraction was repeated before finalization; `scripts/verify-dense-data-goal.mjs` checks DQ-001..DQ-018 and DD-001..DD-011 are present. | Ongoing guard: future completion claims must keep the source audit, matrix, and verifier in sync. |
