# IoT

Standalone local-first IoT control plane for ClawJS.

Local workflow:

```bash
npm --prefix iot ci
npm --prefix iot run build
npm --prefix iot run start
```

Default local URL:

- [http://127.0.0.1:4520](http://127.0.0.1:4520)
