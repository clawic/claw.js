# CLAUDE.md

Read `AGENTS.md` first and treat it as canonical.

- `CLAUDE.md` exists only to redirect Claude Code and Anthropic tooling to the real instructions.
- Keep prompt-based tests text-only and non-operative by default.
- Do not use prompts that can trigger file reads, machine inspection, command execution, edits, or deletions unless the test explicitly isolates that capability.
