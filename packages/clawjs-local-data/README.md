# @clawjs/local-data

Optional local data capability pack for `@clawjs/cli`.

Install this pack when CLI commands need local SQLite-backed data such as
tasks, productivity records, memory, and local database storage.
